coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%

ignore:
  - "examples"
  - "tests"
  - "external"
  - "build*/**"
  - ".build*/**"
  - "cmake-build-*/**"
